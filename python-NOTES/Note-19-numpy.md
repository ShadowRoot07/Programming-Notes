# Numpy

Numpy es la base de la ciencia de datos, su tarea principal es el manejo de arrays (listas especializadas), que son mucho mas rapidas y eficientes que las listas de python.

```python
impprt numpy as np

#crear un array (lista numerica potente)
numeros= np.array([1, 2, 3, 4, 5])

#Operaciones matematicas directas
doble= numeros * 2
media= np.mean(numeros)
```

{Por el momento eso es todo lo que se escubrira}
